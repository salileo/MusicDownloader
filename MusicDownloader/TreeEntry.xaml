<UserControl x:Class="MusicDownloader.TreeEntry"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal">
        <Image VerticalAlignment="Center" Margin="5,0" Width="15" Height="15" Stretch="Uniform" Source="{Binding Path=Icon}"/>
        <TextBlock VerticalAlignment="Center" Margin="0,0,5,0" Text="{Binding Path=Name}" ToolTip="{Binding Path=URL}"/>
    </StackPanel>
</UserControl>
